#include "config.h"


#ifdef

#define ERROR(msg, ...)
#define WARN(msg, ...)
#define INFO(msg, ...)
#define DEBUG(msg, ...)

#else

#define ERROR(msg, ...)
#define WARN(msg, ...)
#define INFO(msg, ...)
#define DEBUG(msg, ...)

#endif
